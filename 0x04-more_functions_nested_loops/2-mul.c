#include "main.h"
/**
  *mul - multiplies two integers
  *@a: a - variable
  *@b: b - variable
  *Return: always 0 (success)
  */
int mul(int a, int b)
{
	int mult;

	mult = a * b;

	return (mult);
}
